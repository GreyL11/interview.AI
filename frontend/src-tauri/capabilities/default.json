{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set. The UI talks to the local backend over plain HTTP and WebSocket, so it needs no filesystem, no HTTP plugin, and no shell access of its own.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open"
  ]
}
